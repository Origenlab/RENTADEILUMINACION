---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/Hero.astro';
import CtaBar from '../../components/CtaBar.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import DirectoryCard from '../../components/DirectoryCard.astro';
import FaqSection from '../../components/FaqSection.astro';
import SectionHeader from '../../components/SectionHeader.astro';

const empresas = await getCollection('directorio', ({ data }) => data.activo !== false);
empresas.sort((a, b) => (b.data.resenas ?? 0) - (a.data.resenas ?? 0));

const cdmxEmpresas = empresas.filter(e => e.data.zona === 'cdmx');
const edomexEmpresas = empresas.filter(e => e.data.zona === 'estado-de-mexico');

const categorias = [
  { key: 'all', label: 'Todos', count: empresas.length },
  { key: 'audio', label: 'Audio', count: empresas.filter(e => e.data.categoria === 'audio').length },
  { key: 'iluminacion', label: 'Iluminación', count: empresas.filter(e => e.data.categoria === 'iluminacion').length },
  { key: 'dj', label: 'DJ', count: empresas.filter(e => e.data.categoria === 'dj').length },
  { key: 'audio-video', label: 'Audio & Video', count: empresas.filter(e => e.data.categoria === 'audio-video').length },
  { key: 'efectos', label: 'Efectos', count: empresas.filter(e => e.data.categoria === 'efectos').length },
];

const topEmpresas = empresas.slice(0, 5);

const title = "Directorio de Proveedores de Audio e Iluminación para Eventos en CDMX | REDEIL";
const description = "Encuentra las mejores tiendas de audio, iluminación y equipo profesional para eventos en Ciudad de México y Estado de México. 25 empresas verificadas con ratings reales.";
const canonical = "https://rentadeiluminacion.com/directorio/";

const schemaMarkup = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": "Directorio de Proveedores de Equipo de Audio e Iluminación CDMX",
  "description": description,
  "numberOfItems": empresas.length,
  "itemListElement": empresas.map((e, i) => ({
    "@type": "ListItem",
    "position": i + 1,
    "name": e.data.nombre,
    "url": `https://rentadeiluminacion.com/directorio/${e.slug}/`
  }))
});

const faqItems = [
  { question: "¿Estas empresas son de REDEIL?", answer: "No. Este directorio lista tiendas independientes que venden equipo de audio e iluminación en CDMX y Estado de México. REDEIL es un servicio de renta — si prefieres rentar equipo con instalación incluida, cotiza con nosotros." },
  { question: "¿Cómo seleccionan las empresas del directorio?", answer: "Buscamos tiendas verificadas en Google Maps con buenas calificaciones y reseñas reales de compradores. Todas están ubicadas en CDMX o Estado de México y se dedican a la venta de equipo profesional." },
  { question: "¿Cuál es la diferencia entre comprar y rentar equipo?", answer: "Comprar es para quien usa equipo frecuentemente (DJs, productores, iglesias). Rentar es ideal para eventos únicos como bodas, XV años o corporativos — incluye transporte, instalación y operación técnica." },
  { question: "¿Puedo rentar equipo con REDEIL en vez de comprar?", answer: "Sí. REDEIL renta iluminación, sonido, DJ y efectos especiales para eventos en toda la zona metropolitana. El servicio incluye transporte, montaje, operación durante el evento y desmontaje. Cotiza gratis por WhatsApp." },
  { question: "¿Cada cuánto actualizan el directorio?", answer: "Verificamos la información periódicamente. Si una empresa cerró, cambió de dirección o tiene datos incorrectos, contáctanos para actualizarla." },
  { question: "¿Cómo puedo agregar mi negocio al directorio?", answer: "Si vendes equipo de audio, iluminación o efectos especiales en CDMX o Estado de México, escríbenos por WhatsApp con los datos de tu negocio y lo revisamos para incluirlo." },
];
---

<BaseLayout title={title} description={description} canonical={canonical} schemaMarkup={schemaMarkup}>

  <Fragment slot="breadcrumbs">
    <Breadcrumbs items={[{ label: "Inicio", href: "/" }, { label: "Directorio" }]} />
  </Fragment>

  <!-- HERO -->
  <Hero
    title="Directorio de Proveedores de Audio e Iluminación"
    subtitle="Las mejores tiendas de equipo profesional para eventos en CDMX y Estado de México"
    introParagraph1="Hemos seleccionado las <strong>25 tiendas mejor calificadas</strong> en Google Maps que venden equipo de sonido, iluminación LED, consolas para DJ y efectos especiales en la Ciudad de México y zona metropolitana. Todas verificadas, con ratings y reseñas reales de compradores."
    introParagraph2="Si lo que buscas es <strong>comprar tu propio equipo</strong>, aquí encontrarás las opciones más confiables. Si prefieres <strong>rentar equipo para un evento</strong> con transporte, instalación y operación incluida, <a href='/contacto/' style='color:#ff8a65;text-decoration:underline'>cotiza con REDEIL</a> — es lo que hacemos mejor."
  />

  <!-- CTA BAR -->

  <!-- ZONAS PRINCIPALES -->
  <section class="dir-zonas">
    <div class="container">
      <SectionHeader
        title="Explora por Zona"
        subtitle="CDMX y Estado de México"
        paragraph1="Nuestro directorio cubre las dos zonas con mayor concentración de tiendas de equipo profesional para eventos: la Ciudad de México con su histórico corredor de audio en el Centro y las tiendas especializadas del Estado de México."
        paragraph2="Selecciona tu zona para ver las tiendas más cercanas a ti, con dirección exacta, teléfono y calificación de Google Maps."
      />
      <div class="dir-zonas__grid">
        <a href="/directorio/cdmx/" class="dir-zona-card">
          <img src="/img/directorio/directorio-fachada-tienda-01.avif" alt="Tiendas de audio e iluminación en CDMX" loading="lazy" width="600" height="340" />
          <div class="dir-zona-card__overlay">
            <h3>Ciudad de México</h3>
            <p class="dir-zona-card__count">{cdmxEmpresas.length} tiendas verificadas</p>
            <p class="dir-zona-card__desc">Centro Histórico, Cuauhtémoc, Benito Juárez, Iztapalapa y más</p>
            <span class="dir-zona-card__cta">Explorar CDMX →</span>
          </div>
        </a>
        <a href="/directorio/estado-de-mexico/" class="dir-zona-card">
          <img src="/img/directorio/directorio-almacen-luces-01.avif" alt="Tiendas de audio e iluminación en Estado de México" loading="lazy" width="600" height="340" />
          <div class="dir-zona-card__overlay">
            <h3>Estado de México</h3>
            <p class="dir-zona-card__count">{edomexEmpresas.length} tiendas verificadas</p>
            <p class="dir-zona-card__desc">Naucalpan, Tlalnepantla, Ecatepec y zona metropolitana</p>
            <span class="dir-zona-card__cta">Explorar Edomex →</span>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- TODAS LAS EMPRESAS -->
  <section class="dir-main" id="empresas">
    <div class="container dir-main__layout">

      <!-- CONTENIDO PRINCIPAL -->
      <div class="dir-main__content">
        <div class="dir-main__header">
          <h2 class="dir-main__title">{empresas.length} Proveedores Verificados</h2>
          <!-- Category pills -->
          <div class="dir-pills" id="dirPills">
            {categorias.map(cat => (
              <button class={`dir-pill ${cat.key === 'all' ? 'dir-pill--active' : ''}`} data-cat={cat.key}>
                {cat.label} <span class="dir-pill__count">{cat.count}</span>
              </button>
            ))}
          </div>
        </div>

        <div class="services-grid" id="dirGrid">
          {empresas.map(empresa => (
            <div class="dir-item" data-categoria={empresa.data.categoria} data-zona={empresa.data.zona}>
              <DirectoryCard
                nombre={empresa.data.nombre}
                categoria={empresa.data.categoria}
                zona={empresa.data.zona}
                alcaldia_municipio={empresa.data.alcaldia_municipio}
                telefono={empresa.data.telefono}
                rating={empresa.data.rating}
                resenas={empresa.data.resenas}
                slug={empresa.slug}
              />
            </div>
          ))}
        </div>
      </div>

      <!-- SIDEBAR -->
      <aside class="dir-sidebar">
        <!-- Top 5 -->
        <div class="dir-sidebar__box">
          <h3 class="dir-sidebar__title">⭐ Mejor Calificadas</h3>
          <ul class="dir-sidebar__list">
            {topEmpresas.map(e => (
              <li class="dir-sidebar__item">
                <a href={`/directorio/${e.slug}/`}>
                  <span class="dir-sidebar__name">{e.data.nombre}</span>
                  <span class="dir-sidebar__meta">⭐ {e.data.rating} ({e.data.resenas})</span>
                </a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Categorías -->
        <div class="dir-sidebar__box">
          <h3 class="dir-sidebar__title">📂 Categorías</h3>
          <ul class="dir-sidebar__list">
            {categorias.filter(c => c.key !== 'all').map(cat => (
              <li class="dir-sidebar__item">
                <button class="dir-sidebar__cat-btn" data-filter={cat.key}>
                  {cat.label} <span>({cat.count})</span>
                </button>
              </li>
            ))}
          </ul>
        </div>

        <!-- CTA REDEIL -->
        <div class="dir-sidebar__cta">
          <h3>¿Prefieres Rentar?</h3>
          <p>No necesitas comprar equipo para tu evento. REDEIL te lo lleva, instala y opera.</p>
          <a href="/contacto/" class="btn btn-primary btn-lg">Cotiza Gratis →</a>
          <a href="https://wa.me/525530682988?text=Hola,%20quiero%20cotizar%20equipo%20para%20mi%20evento" target="_blank" rel="noopener noreferrer" class="btn btn-whatsapp">
            WhatsApp — 55 3068 2988
          </a>
        </div>

        <!-- Zonas -->
        <div class="dir-sidebar__box">
          <h3 class="dir-sidebar__title">📍 Por Zona</h3>
          <ul class="dir-sidebar__list">
            <li class="dir-sidebar__item">
              <a href="/directorio/cdmx/">CDMX <span>({cdmxEmpresas.length})</span></a>
            </li>
            <li class="dir-sidebar__item">
              <a href="/directorio/estado-de-mexico/">Estado de México <span>({edomexEmpresas.length})</span></a>
            </li>
          </ul>
        </div>
      </aside>

    </div>
  </section>

  <!-- FAQ -->
  <FaqSection items={faqItems} />

  <!-- CTA FINAL -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content" style="text-align:center; max-width:640px; margin:0 auto;">
        <h2>¿Prefieres que nos encarguemos de todo?</h2>
        <p>En REDEIL rentamos equipo de iluminación, sonido, DJ y efectos especiales para tu evento. Transporte, montaje y operación incluidos.</p>
        <div style="display:flex; gap:1rem; justify-content:center; flex-wrap:wrap; margin-top:1.5rem;">
          <a href="/contacto/" class="btn btn-primary btn-lg">Cotizar Evento →</a>
          <a href="/servicios/" class="btn btn-outline btn-lg">Ver Servicios</a>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<script is:inline>
document.addEventListener('DOMContentLoaded', function() {
  var pills = document.querySelectorAll('.dir-pill, .dir-sidebar__cat-btn');
  var items = document.querySelectorAll('#dirGrid .dir-item');
  
  pills.forEach(function(pill) {
    pill.addEventListener('click', function() {
      var cat = this.dataset.cat || this.dataset.filter;
      
      // Update active pill
      document.querySelectorAll('.dir-pill').forEach(function(p) { p.classList.remove('dir-pill--active'); });
      var mainPill = document.querySelector('.dir-pill[data-cat="' + cat + '"]');
      if (mainPill) mainPill.classList.add('dir-pill--active');
      
      // Filter items
      items.forEach(function(item) {
        if (cat === 'all' || item.dataset.categoria === cat) {
          item.style.display = '';
        } else {
          item.style.display = 'none';
        }
      });
    });
  });
});
</script>

<!-- Estilos en /css/style.css (global) — evita FOUC en dev mode -->
